# tests/assets/scenes/mirror_sphere.test.toml
# Regression-test parameters for mirror_sphere.yaml.

[render]
width  = 256
height = 256
spp    = 256          # samples per pixel

[regression]
threshold_mse  = 0.002   # mean squared error (linear, 0-1 range)
threshold_psnr = 38.0    # minimum Peak Signal-to-Noise Ratio (dB)
